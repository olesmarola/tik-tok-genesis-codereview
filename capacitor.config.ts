import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tiktuk-genesis-test',
  webDir: 'out',
  bundledWebRuntime: false
};

export default config;
